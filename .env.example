# =============================================================================
# Parametric Insurance Demo — Environment Variables
# =============================================================================
# These values can be set as environment variables, Fabric pipeline parameters,
# or edited directly in the notebook's DemoConfig class (Step 0).
# =============================================================================

# -- Azure Event Grid (Optional) --
# Leave blank to run in local-only mode (events logged but not published).
# Get these values from: az eventgrid topic show / az eventgrid topic key list
EVENTGRID_TOPIC_ENDPOINT=
EVENTGRID_KEY=

# -- Microsoft Foundry / Azure OpenAI (Optional) --
# Leave blank to use the built-in rule-based claim validation engine.
FOUNDRY_ENDPOINT=
FOUNDRY_API_KEY=

# -- Azure Resources (created by setup/azure-setup.sh) --
AZURE_SUBSCRIPTION_ID=
RESOURCE_GROUP=parametric-insurance-rg
LOCATION=eastus
FUNCTION_APP_NAME=
STORAGE_ACCOUNT_NAME=
APPINSIGHTS_INSTRUMENTATIONKEY=

# -- Microsoft Fabric (set after creating workspace/lakehouse) --
FABRIC_WORKSPACE_ID=
FABRIC_LAKEHOUSE_ID=
FABRIC_WAREHOUSE_SERVER=
FABRIC_DATABASE=parametric_insurance_warehouse
